#!/usr/bin/env python3

import random
import sys

#with open(sys.stdin) as f:
text = sys.stdin.read()
for i in range(len(text)):#range(0, len(text), 2):
   if random.random() > 0.5:
      text = text[:i] + text[i].upper() + text[i + 1:]
print(text)
